from setuptools import setup, find_packages

setup(
    name="zerolm",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "gradio",
        # Add other dependencies here
    ],
)